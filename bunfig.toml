# Bun configuration for tests (monorepo root)
[test]
# Increase default timeout to 10 seconds for async component tests
timeout = 10000
# Preload test setup file to configure React Testing Library
preload = ["./test-setup.ts"]
