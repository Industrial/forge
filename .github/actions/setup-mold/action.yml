# Install mold linker for faster linking on Linux.
description: "Install mold linker (Linux)"
inputs: {}
outputs: {}
runs:
  using: "composite"
  steps:
    - name: Install mold linker
      run: |
        sudo apt-get update
        sudo apt-get install -y mold
