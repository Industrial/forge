# 001_cli — Forge application for CLI tests

This directory represents one Forge application used by the `001_cli` integration test.

Tests run by invoking the Forge CLI and observing/expecting the result (exit code, stdout, stderr).
