language: rust
type: library
tasks:
  build:
    command: cargo
    args:
    - build
    - -p
    - forge-cron
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    deps:
    - forge-cron:lint
  check:
    command: cargo
    args:
    - check
    - -p
    - forge-cron
    - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
  test:
    command: cargo
    args:
    - nextest
    - run
    - -p
    - forge-cron
    - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    - '**/*.toml'
    deps:
    - forge-cron:build
  lint:
    command: cargo
    args:
    - clippy
    - -p
    - forge-cron
    - --all-targets
    - --all-features
    - --
    - -D
    - warnings
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    deps:
    - forge-cron:check
