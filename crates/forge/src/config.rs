//! Configuration (re-exported from [forge_config](forge_config)).

pub use forge_config::{
  AppConfig, DatabaseConfig, ForgeConfig, FrontendConfig, ServerConfig, effective_environment,
  load_config,
};
