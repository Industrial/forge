language: rust
type: library
tasks:
  build:
    command: cargo
    args:
    - build
    - -p
    - forge-observability
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    deps:
    - forge-observability:lint
  check:
    command: cargo
    args:
    - check
    - -p
    - forge-observability
    - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
  test:
    command: cargo
    args:
    - nextest
    - run
    - -p
    - forge-observability
    - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    - '**/*.toml'
    deps:
    - forge-observability:build
  lint:
    command: cargo
    args:
    - clippy
    - -p
    - forge-observability
    - --all-targets
    - --all-features
    - --
    - -D
    - warnings
    options:
      runFromWorkspaceRoot: true
    inputs:
    - Cargo.toml
    - Cargo.lock
    - '**/*.rs'
    deps:
    - forge-observability:check
