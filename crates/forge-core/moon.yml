language: rust
type: library
tasks:
  build:
    command: cargo
    args:
      - build
      - -p
      - forge-core
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge-core:lint
  check:
    command: cargo
    args:
      - check
      - -p
      - forge-core
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
  test:
    command: cargo
    args:
      - nextest
      - run
      - -p
      - forge-core
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
      - '**/*.toml'
    deps:
      - forge-core:build
  lint:
    command: cargo
    args:
      - clippy
      - -p
      - forge-core
      - --all-targets
      - --all-features
      - --
      - -D
      - warnings
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge:format
      - forge-core:check
