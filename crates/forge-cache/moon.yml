language: rust
type: library
tasks:
  build:
    command: cargo
    args:
      - build
      - -p
      - forge-cache
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
    deps:
      - forge-cache:lint
  check:
    command: cargo
    args:
      - check
      - -p
      - forge-cache
      - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
  test:
    command: cargo
    args:
      - nextest
      - run
      - -p
      - forge-cache
      - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
      - '**/*.toml'
    deps:
      - forge-cache:build
  lint:
    command: cargo
    args:
      - clippy
      - -p
      - forge-cache
      - --all-targets
      - --all-features
      - --
      - -D
      - warnings
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
    deps:
      - forge-cache:check
