# test-e2e

Playwright E2E tests. The test runner starts the API and Vite dev server, then runs
Playwright from this directory.

- **Run:** `moon test-e2e` or `bash bin/test-e2e` from project root.
- **Location:** `tests/*.ts`, `helpers/` in this directory.
