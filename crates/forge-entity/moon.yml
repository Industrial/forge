language: rust
type: library
tasks:
  build:
    command: cargo
    args:
      - build
      - -p
      - forge-entity
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge-entity:lint
  check:
    command: cargo
    args:
      - check
      - -p
      - forge-entity
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
  test:
    command: cargo
    args:
      - nextest
      - run
      - -p
      - forge-entity
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
      - '**/*.toml'
    deps:
      - forge-entity:build
  lint:
    command: cargo
    args:
      - clippy
      - -p
      - forge-entity
      - --all-targets
      - --all-features
      - --
      - -D
      - warnings
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge:format
      - forge-entity:check
