language: rust
type: library
tasks:
  build:
    command: cargo
    args:
      - build
      - -p
      - forge-auth
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge-auth:lint
  check:
    command: cargo
    args:
      - check
      - -p
      - forge-auth
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
  test:
    command: cargo
    args:
      - nextest
      - run
      - -p
      - forge-auth
      - --all-features
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
      - '**/*.toml'
    deps:
      - forge-auth:build
  lint:
    command: cargo
    args:
      - clippy
      - -p
      - forge-auth
      - --all-targets
      - --all-features
      - --
      - -D
      - warnings
    options:
      outputStyle: buffer-only-failure
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - /Cargo.lock
      - '**/*.rs'
    deps:
      - forge:format
      - forge-auth:check
