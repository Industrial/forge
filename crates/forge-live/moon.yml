language: rust
type: library
tasks:
  build:
    command: cargo
    args:
      - build
      - -p
      - forge-live
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
    deps:
      - forge-live:lint
  check:
    command: cargo
    args:
      - check
      - -p
      - forge-live
      - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
  test:
    command: cargo
    args:
      - nextest
      - run
      - -p
      - forge-live
      - --all-features
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
      - '**/*.toml'
    deps:
      - forge-live:build
  lint:
    command: cargo
    args:
      - clippy
      - -p
      - forge-live
      - --all-targets
      - --all-features
      - --
      - -D
      - warnings
    options:
      runFromWorkspaceRoot: true
    inputs:
      - Cargo.toml
      - Cargo.lock
      - '**/*.rs'
    deps:
      - forge:format
      - forge-live:check
